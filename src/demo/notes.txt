svg-> scalacble vector graphic, 2d image representation in xml language

svg -> top to bottom incs while x axis left to right incs as usual.

d3.select() -> by tag name, or by class name(select the first one.)

de.selectAll -> to select all the same tag.

Data binding:   
    datum-> bind entire data set to single. i.e for one to one binding.

    data-> array of data in all selection of data uniformly.

.enter() -> create elements for missing databset
attr() -> set attributes dynammically

const scale = d3.scaleLinear().domain([0, 100]).range([0, 500]);

console.log(scale(50)); // Maps 50 to 250
domain([min, max]): Input range
range([min, max]): Output range

attr('x', (d,i)=> i*50)
attr(height, d);
attr('fill', "pink")

creating dom element using data:
    svg.selectAll('dom element')
        .data(barData)
        .enter()
        .append('dom element')
    The two dom element should match.
    barData should be an array..  